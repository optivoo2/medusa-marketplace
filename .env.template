# Configurações obrigatórias
# Ajuste as origens permitidas conforme os domínios oficiais do projeto
STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:5173
AUTH_CORS=http://localhost:5173

# Recomenda-se usar Redis gerenciado (inclua usuário/senha se necessário)
REDIS_URL=redis://localhost:6379

# Defina segredos fortes com pelo menos 32 caracteres
JWT_SECRET=
COOKIE_SECRET=

# Banco de dados PostgreSQL (veja README para string de conexão)
DATABASE_URL=
DB_NAME=medusa-v2

# Configurações adicionais
PORT=9000
MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_WORKER_MODE=server
